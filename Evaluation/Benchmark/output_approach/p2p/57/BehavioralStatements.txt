#statements:
6
In the log, after "Purchase SC", "Approve SC" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5444]"
In the log, after "Approve SC", "Create PO" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5444]"
In the log, after "Approve SC", "Create PO" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5452]"
In the log, after "Create PO", "Approve SC" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5452]"
In the log, after "Purchase SC", "Approve SC" is repeated while in the model it is not
In the log, after "Approve SC", "Create PO" is repeated while in the model it is not
