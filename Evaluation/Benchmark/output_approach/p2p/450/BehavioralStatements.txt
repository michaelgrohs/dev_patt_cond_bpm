#statements:
9
In the log, after "Release PR", "Create PO" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5452]"
In the log, after "Create PO", "Approve PO 1" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5452]"
In the log, after "Approve PO 1", "Release PR" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5452]"
In the log, after "Create PO", "Approve PO 1" occurs before task "Release PR", while in the model they are mutually exclusive after marking "[ent_element5442]"
In the log, after "Create PO", "Approve PO 1" occurs before task "Create PO", while in the model they are mutually exclusive after marking "[ent_element5442]"
In the log, after "Create PO", "Approve PO 1" occurs before task "Approve PO 1", while in the model they are mutually exclusive after marking "[ent_element5442]"
In the log, after "Create PR", "Release PR" is repeated while in the model it is not
In the log, after "Release PR", "Create PO" is repeated while in the model it is not
In the log, after "Create PO", "Approve PO 1" is repeated while in the model it is not
