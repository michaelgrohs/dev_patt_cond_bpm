#statements:
6
In the log, after "Activity E", "Activity G" occurs before task "Activity G", while in the model they are mutually exclusive after marking "[exi_element2734]"
In the log, after "Activity G", "Activity D" occurs before task "Activity G", while in the model they are mutually exclusive after marking "[exi_element2734]"
In the log, after "Activity D", "Activity E" occurs before task "Activity G", while in the model they are mutually exclusive after marking "[exi_element2734]"
In the log, after "Activity C", "Activity D" is repeated while in the model it is not
In the log, after "Activity D", "Activity E" is repeated while in the model it is not
In the log, after "Activity E", "Activity G" is repeated while in the model it is not
