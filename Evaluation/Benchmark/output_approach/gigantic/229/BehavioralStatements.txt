#statements:
9
In the log, after "Activity BE", "Activity BQ" occurs before task "Activity BR", while in the model they are mutually exclusive after marking "[ent_element30166]"
In the log, after "Activity BQ", "Activity BR" occurs before task "Activity BR", while in the model they are mutually exclusive after marking "[ent_element30166]"
In the log, after "Activity BR", "Activity BE" occurs before task "Activity BR", while in the model they are mutually exclusive after marking "[ent_element30166]"
In the log, after "Activity BQ", "Activity BR" occurs before task "Activity BE", while in the model they are mutually exclusive after marking "[ent_element30129]"
In the log, after "Activity BQ", "Activity BR" occurs before task "Activity BQ", while in the model they are mutually exclusive after marking "[ent_element30129]"
In the log, after "Activity BQ", "Activity BR" occurs before task "Activity BR", while in the model they are mutually exclusive after marking "[ent_element30129]"
In the log, after "Activity BQ", "Activity BR" is repeated while in the model it is not
In the log, after "Activity BG", "Activity BE" is repeated while in the model it is not
In the log, after "Activity BE", "Activity BQ" is repeated while in the model it is not
